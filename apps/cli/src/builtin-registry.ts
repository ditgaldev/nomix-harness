/** Source-launch placeholder; npm distribution generation replaces this module. */

import type { Context } from '@nomix-ai/cordis'

/** Source workspaces resolve package names directly, so no packaged builtins are required. */
export function registerNomixBuiltins(_ctx: Context): void {}
