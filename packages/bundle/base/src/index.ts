/**
 * @nomix-ai/nomix-base — the shared nomix core as a profile bundle. The
 * package's substance is `cordis.patch.yml`, declared by the `nomix.bundle.patch`
 * manifest field and resolved by the profile composer through that field;
 * this module carries no runtime API.
 * @module @nomix-ai/nomix-base
 */

export {}
