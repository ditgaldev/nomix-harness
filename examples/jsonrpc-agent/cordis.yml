# Unattended coding-agent deployment for the bundled nomix-jsonrpc-agent runtime.
# stdout is reserved for JSON-RPC; do not add a console logger or terminal UI.

- id: sdk-jsonrpc-server
  name: '@nomix-ai/nomix-sdk-jsonrpc-server'
  config:
    maxTokensAsSuccess: !!js "process.env.NOMIX_MAX_TOKENS_AS_SUCCESS === undefined ? true : JSON.parse(process.env.NOMIX_MAX_TOKENS_AS_SUCCESS)"

# The DeepSeek adapter. Shipped default: full thinking at max effort on every
# request; exact-model resolution materializes request defaults before logging.
# The model arrives per session over JSON-RPC, so it is not pinned here.
- id: llm-deepseek
  name: '@nomix-ai/nomix-llm-deepseek'
  config:
    thinking: enabled
    reasoningEffort: max

# Managed child-process groups for the bash executor (spawn/kill/output plumbing).
- id: subprocess
  name: '@nomix-ai/nomix-subprocess-local'

- id: bash
  name: '@nomix-ai/nomix-bash-local'
  config:
    cwd: !!js process.env.NOMIX_CWD ?? process.cwd()
    timeoutMs: 60000

- id: agent-spine
  name: '@nomix-ai/nomix-agent-spine-demo'
  config:
    persona: !!js process.env.NOMIX_SYSTEM_PROMPT ?? 'You are a coding agent.'
    workspaceContext: false
    skills:
      enabled: false
    toolBash:
      enableRunInBackground: false
    toolJobs: false

- id: sessions
  name: '@nomix-ai/nomix-session-persistence-jsonl'
  config:
    root: !!js process.env.NOMIX_SESSION_ROOT ?? './.sessions'
    # Snapshot runs read the raw JSONL back; production keeps zstd frames.
    compression: !!js "process.env.NOMIX_SNAPSHOT === undefined ? 'zstd' : 'none'"

- id: session-checkpoints
  name: '@nomix-ai/nomix-session-checkpoint-policy'

- id: subagent
  name: '@nomix-ai/nomix-subagent'

- id: subagent-spawn-in-process
  name: '@nomix-ai/nomix-subagent-spawn-in-process'
  config:
    providerName: spawn

- id: tool-subagent
  name: '@nomix-ai/nomix-tool-subagent'
  config:
    provider: spawn
    toolName: subagent
    enableRunInBackground: false

- id: tool-todo
  name: '@nomix-ai/nomix-tool-todo'
  config:
    allowParallelInProgress: true

- id: fs-local
  name: '@nomix-ai/nomix-fs-local'
  config:
    cwd: !!js process.env.NOMIX_CWD ?? process.cwd()

- id: fs-observation-policy
  name: '@nomix-ai/nomix-fs-observation-policy'

- id: tool-fs
  name: '@nomix-ai/nomix-tool-fs'

- id: token-meter
  name: '@nomix-ai/nomix-token-meter'

- id: compaction-basic
  name: '@nomix-ai/nomix-compaction-basic'
  config:
    thresholdRatio: 0.8
    retainRatio: 0.16
    maxTokens: 8192
    compactionRetries: 1
