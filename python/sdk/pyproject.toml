[build-system]
requires = ["hatchling==1.30.1"]
build-backend = "hatchling.build"

[project]
name = "nomix-harness-sdk"
version = "0.0.0.dev0"
description = "Python SDK for Nomix Harness"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "Nomix AI" }]
dependencies = [
  "pydantic>=2.12,<3",
  "nomix-harness-runtime-bin==0.0.0.dev0",
]

[project.urls]
Homepage = "https://github.com/ditgaldev/nomix-harness"
Documentation = "https://github.com/ditgaldev/nomix-harness/blob/master/docs/user/guide/python-sdk.md"
Issues = "https://github.com/ditgaldev/nomix-harness/issues"
Source = "https://github.com/ditgaldev/nomix-harness"

[dependency-groups]
test = ["pytest>=8.0"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.hatch.build.targets.wheel]
packages = ["src/nomix_harness"]

# Editable installs see runtime executables injected after installation instead
# of freezing a wheel snapshot.
[tool.uv.sources]
nomix-harness-runtime-bin = { path = "../sdk-runtime", editable = true }
